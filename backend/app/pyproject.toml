[project]
name = "microservices-python"
version = "0.1.0"
description = "FastAPI microservices workspace"
requires-python = ">=3.12"

[tool.uv.sources]
shared = { workspace = true }

[tool.uv.workspace]
members = ["shared", "gateway", "services/*"]